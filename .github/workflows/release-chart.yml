# Release the Helm chart as an OCI artifact to ghcr.io.
# Triggered manually via workflow_dispatch or called by other workflows.

name: Release Helm Chart

run-name: "Release Chart ${{ inputs.version_override || format('{0} bump', inputs.bump_type) }}"

on:
  workflow_dispatch:
    inputs:
      bump_type:
        description: "Chart version bump type"
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
      version_override:
        description: "Override chart version (e.g. 0.2.0) — ignores bump_type"
        required: false
        type: string
      app_version_override:
        description: "Override appVersion (default: auto-detect latest v* tag)"
        required: false
        type: string
  workflow_call:
    inputs:
      bump_type:
        type: string
        default: "patch"
      version_override:
        type: string
        default: ""
      app_version_override:
        type: string
        default: ""

concurrency:
  group: release-chart
  cancel-in-progress: false

permissions:
  contents: write
  packages: write

jobs:
  release:
    name: Release Chart
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Compute next chart version
        id: version
        run: |
          LATEST_TAG=$(git tag -l 'chart-v[0-9]*.[0-9]*.[0-9]*' | sort -V | tail -n1 || true)

          if [ -z "$LATEST_TAG" ]; then
            MAJOR=0; MINOR=0; PATCH=0
            echo "No existing chart-v tags, starting from 0.0.0"
          else
            LATEST_VERSION="${LATEST_TAG#chart-v}"
            MAJOR=$(echo "$LATEST_VERSION" | cut -d. -f1)
            MINOR=$(echo "$LATEST_VERSION" | cut -d. -f2)
            PATCH=$(echo "$LATEST_VERSION" | cut -d. -f3)
            echo "Latest chart tag: $LATEST_TAG"
          fi

          OVERRIDE="${{ inputs.version_override }}"
          if [ -n "$OVERRIDE" ]; then
            if ! echo "$OVERRIDE" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+$'; then
              echo "::error::Invalid version format '$OVERRIDE'. Expected: X.Y.Z"
              exit 1
            fi
            NEXT_VERSION="$OVERRIDE"
          else
            case "${{ inputs.bump_type }}" in
              major) NEXT_VERSION="$((MAJOR + 1)).0.0" ;;
              minor) NEXT_VERSION="${MAJOR}.$((MINOR + 1)).0" ;;
              patch) NEXT_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))" ;;
            esac
          fi

          NEXT_TAG="chart-v${NEXT_VERSION}"

          if git rev-parse "$NEXT_TAG" >/dev/null 2>&1; then
            echo "::error::Tag $NEXT_TAG already exists"
            exit 1
          fi

          echo "version=${NEXT_VERSION}" >> $GITHUB_OUTPUT
          echo "tag=${NEXT_TAG}" >> $GITHUB_OUTPUT
          echo "Next chart: $NEXT_VERSION (tag: $NEXT_TAG)"

      - name: Detect appVersion
        id: appversion
        run: |
          OVERRIDE="${{ inputs.app_version_override }}"
          if [ -n "$OVERRIDE" ]; then
            echo "app_version=${OVERRIDE}" >> $GITHUB_OUTPUT
            echo "Using override appVersion: $OVERRIDE"
          else
            LATEST_APP_TAG=$(git tag -l 'v[0-9]*.[0-9]*.[0-9]*' | sort -V | tail -n1 || true)
            if [ -n "$LATEST_APP_TAG" ]; then
              APP_VERSION="${LATEST_APP_TAG#v}"
              echo "app_version=${APP_VERSION}" >> $GITHUB_OUTPUT
              echo "Auto-detected appVersion from $LATEST_APP_TAG: $APP_VERSION"
            else
              echo "app_version=0.0.0" >> $GITHUB_OUTPUT
              echo "No v* tags found, using appVersion: 0.0.0"
            fi
          fi

      - name: Update Chart.yaml
        run: |
          yq -i ".version = \"${{ steps.version.outputs.version }}\"" deploy/helm/home-wiki/Chart.yaml
          yq -i ".appVersion = \"${{ steps.appversion.outputs.app_version }}\"" deploy/helm/home-wiki/Chart.yaml
          echo "Updated Chart.yaml:"
          cat deploy/helm/home-wiki/Chart.yaml

      - name: Commit and tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add deploy/helm/home-wiki/Chart.yaml
          git commit -m "chore(chart): release ${{ steps.version.outputs.version }} [skip ci]"
          git tag -a "${{ steps.version.outputs.tag }}" -m "Chart ${{ steps.version.outputs.version }}"
          git push origin main "${{ steps.version.outputs.tag }}"

      - uses: azure/setup-helm@v4

      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Package and push
        run: |
          helm package deploy/helm/home-wiki/
          helm push home-wiki-*.tgz oci://ghcr.io/${{ github.repository_owner }}/charts

      - name: Create GitHub Release
        run: |
          gh release create "${{ steps.version.outputs.tag }}" \
            --title "Chart ${{ steps.version.outputs.version }}" \
            --notes "Helm chart version ${{ steps.version.outputs.version }} with appVersion ${{ steps.appversion.outputs.app_version }}"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Write summary
        run: |
          echo "## Released Chart ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Chart Version | \`${{ steps.version.outputs.version }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| App Version | \`${{ steps.appversion.outputs.app_version }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Tag | \`${{ steps.version.outputs.tag }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| OCI | \`oci://ghcr.io/${{ github.repository_owner }}/charts/home-wiki:${{ steps.version.outputs.version }}\` |" >> $GITHUB_STEP_SUMMARY
